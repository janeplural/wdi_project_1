hello world</br>

<%= link_to "Back", "/users/#{current_user.id}" %></br>

<%= @piece.title %></br>

<% if @piece.subtitle != nil %>
	<%= @piece.subtitle %></br>
<% end %>

<%= @piece.cost %></br>

<% if @piece.thumbnail != nil %>
	<%= image_tag("#{@piece.thumbnail}") %></br>
<% end %>

<% if @piece.pub_date != nil %>
	<%= @piece.pub_date %></br>
<% end %>

<%= link_to "Read Preview of #{@piece.title}", "#{@piece.preview}" %></br>

<% if @piece.description != nil %>
	<%= @piece.description %></br>
<% end %>

<%= link_to "Back", "/users/#{current_user.id}" %>
